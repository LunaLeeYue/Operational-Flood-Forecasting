# Forecast data

`scripts/run_daily.py` generates `catalog.json` and the per-region forecast
assets in this directory. They are included in the GitHub Pages deployment
artifact but are intentionally not committed to Git.
